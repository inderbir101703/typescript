/*type script provides the ability to declare any variables that are not present within the typescript

code base, for example, for the process variable.

We can simply declare it as a variable of type.

Any the syntax for a type declaration is the same as the syntax for declaring any of the variable within */
//can not provide implemntation in declaration
//dec files name==file.d.ts
//npm i @types/node instal type dec files
declare const process:any